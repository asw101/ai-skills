package local:github;

interface api {
    record user-info {
        login: string,
        id: u64,
        name: option<string>,
        bio: option<string>,
        public-repos: u32,
        followers: u32,
        following: u32,
        html-url: string,
    }

    record repo-info {
        full-name: string,
        description: option<string>,
        stargazers-count: u32,
        forks-count: u32,
        language: option<string>,
        default-branch: string,
        html-url: string,
    }

    /// Fetch basic details for a GitHub user.
    /// `token` is an optional bearer credential — pass none for unauthenticated calls.
    get-user: func(login: string, token: option<string>) -> result<user-info, string>;

    /// Fetch basic details for a GitHub repository.
    get-repo: func(owner: string, repo: string, token: option<string>) -> result<repo-info, string>;
}

world github {
    import wasi:http/types@0.2.0;
    import wasi:http/outgoing-handler@0.2.0;
    export api;
}
