package component:tech-ticker;

world tech-ticker {
    export ticker;
}

interface ticker {
    // Simple health check for the component.
    ping: func() -> string;
    
    // Generate a random string of the specified length.
    random-string: func(length: u32) -> string;
}
